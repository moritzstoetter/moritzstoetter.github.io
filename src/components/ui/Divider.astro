---
// A thin full-bleed hairline used to separate home-page sections. It spans the
// full viewport width (matching the navbar bar and hero band) and sits in the
// whitespace between two sections. The colour matches the navbar border so
// dividers read as the same quiet system line.
---

<hr
  class="full-bleed my-0 border-0 border-t border-primaryLight/10 dark:border-neutral-700"
  role="presentation"
/>
