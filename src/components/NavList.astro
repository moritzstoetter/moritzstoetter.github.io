---
import { getLangFromUrl, useTranslations } from "../i18n/utils";

const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);

const alignment: "horizontal" | "vertical" = Astro.props.alignment;
---

<ul
  class=`${alignment === "horizontal" ? "flex-row items-center" : "flex-col"} flex gap-4 text-sm"`
>
  <li>
    <a
      data-spy-link="expertise"
      href={`/${lang}/expertise/`}
      class={`text-lg ${
        Astro.url.pathname === `/${lang}/expertise/`
          ? "text-primaryLight dark:text-primaryDark"
          : ""
      }`}
    >
      {t("nav.expertise")}
    </a>
  </li>
  <li>
    <a
      data-spy-link="case-studies"
      href={`/${lang}/case-studies/`}
      class={`text-lg ${
        Astro.url.pathname === `/${lang}/case-studies/`
          ? "text-primaryLight dark:text-primaryDark"
          : ""
      }`}
    >
      {t("nav.caseStudies")}
    </a>
  </li>
  <li>
    <a
      data-spy-link="insights"
      href={`/${lang}/insights/`}
      class={`text-lg ${
        Astro.url.pathname === `/${lang}/insights/`
          ? "text-primaryLight dark:text-primaryDark"
          : ""
      }`}
    >
      {t("nav.insights")}
    </a>
  </li>
  <li>
    <a
      data-spy-link="contact"
      href={`/${lang}/contact/`}
      class={`text-lg ${
        Astro.url.pathname === `/${lang}/contact/`
          ? "text-primaryLight dark:text-primaryDark"
          : ""
      }`}
    >
      {t("nav.contact")}
    </a>
  </li>
  <li>
    <a
      href={`/${lang}/about/`}
      class={`text-lg ${
        Astro.url.pathname === `/${lang}/about/`
          ? "text-primaryLight dark:text-primaryDark"
          : ""
      }`}
    >
      {t("nav.about")}
    </a>
  </li>
</ul>
